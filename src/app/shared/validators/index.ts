import { IsValidEmailValidator } from "./valid-email.validator";
import { IsValidUrlValidator } from "./valid-url.validator";

export const CustomValidators = {
    IsValidEmailValidator,
    IsValidUrlValidator
}